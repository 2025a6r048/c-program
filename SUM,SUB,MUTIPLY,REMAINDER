include<stdio.h>
int main(){
    int x,y,add,sub,multiply,remainder;
    printf("enter the value of x: ");
    scanf("%d", &x);
    printf("enter the value of y: ");
    scanf("%d", &y);
    add=x+y;
   sub=x-y;
   multiply=x*Y;
remainder=x%y;
printf("add=%d\n subtract=%d\n multiply=%d\n remainder=%d", add,sub,multiply,remainder);
return 0;}
